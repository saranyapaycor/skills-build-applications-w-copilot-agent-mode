# Required: set to your Codespace name so the frontend can reach the backend API.
# Example: if your Codespace URL is https://my-space-8000.app.github.dev
# then VITE_CODESPACE_NAME=my-space
#
# When unset, the frontend falls back to http://localhost:8000 automatically.
VITE_CODESPACE_NAME=
